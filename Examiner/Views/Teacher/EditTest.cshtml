@model Examiner.WEB.ViewModels.TestViewModel

<div asp-validation-summary="All" class="text-danger"></div>

<form asp-action="EditTest" method="post">
    <div class="form-group">
        <input type="hidden" name="testId" value="@Model.Id" />
        <label for="title">Edit test</label>
        <input name="title" class="form-control col-lg-3" value="@Model.Title" />
    </div>
    <div class="form-row">
        <div class="col-1">
            <button type="submit" class="btn btn-primary form-control">Update</button>
        </div>
        <div class="col-1">
            <a asp-action="Tests" class="btn btn-success form-control">Back</a>
        </div>
    </div>
</form>